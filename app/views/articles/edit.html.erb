<%= render 'layouts/tags_input_style' %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-12">

      <div class="col-12 text-center text-danger animated shake">
        <%= flash[:invalid_article] %>
      </div>

      <p class="text-center my-3">
        <span class="bg-navy text-white p-2 rounded">記事編集</span>
      </p>

      <%= simple_form_for(@article) do |f| %>

        <%= render 'articles/article_form', article: @article,
                                            f: f,
                                            article_type: "article",
                                            submit_label: "下書きとして投稿を取り下げる",
                                            submit_name: "create_draft",
                                            submit_confirm: "下書きとして投稿を取り下げると、この記事に対するイイねとキープとコメントが削除され、あなたのEPが減ります。\n投稿の取り下げ -5EP\nイイねの削除 -#{@article.favorites.count}EP\nキープの削除 -#{@article.keeps.count}EP\n本当に下書きとして投稿を取り下げますか？" %>

      <% end %>

      </div>
  </div>
</div>

<%= render 'layouts/tags_input_script' %>
